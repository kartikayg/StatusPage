/**
 * this test will behave more like an integeration test
 */

import {assert} from 'chai';

describe ('index', function () {

  // publish message on queues and check them in the log files.
    // if not publishing object, it shouldn't log

  // log a message using default logger see that appears in the file and/or console

});