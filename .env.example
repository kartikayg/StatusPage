########################
#### SERVER CONF ####
######################

NODE_ENV=development   ## development, production, test
PORT=6040


#########################
#### LOGGING CONF ####
#########################

## Writers available for logging throughout the application. For each
## writer, specify the level of message and it will log any message with
## that or higher level. Omit the entry if you don't want to log using that writer.
## Three writers supported: Console, Database, File
##
## available levels: debug, info, warn, error

LOG_CONSOLE_LEVEL=debug ## outputs to console.
LOG_DB_LEVEL=warn 		## outputs to a db collection (logs). This uses the mongo db connection specified below.
LOG_FILE_LEVEL=error 	## outputs to a file. The log file is rotated daily and is removed after 7 days.

## Extra vars for file logging. 
LOG_FILE_DIRNAME=logs/   ## directory to hold the log files. The dir must be created from before.
LOG_FILE_PREFIX=log		 ## prefix to prepend the filename. defaults to 'log'


## Specify which writer to use to log all http requests coming to this service.
## options: file | db | console

LOG_HTTP_REQUEST_WRITER=file



#######################
#### DB CONF ####
#######################

## MONGO Db endpoint. This should be full url with the database name
MONGO_ENDPOINT=mongodb://dave:password@localhost:27017/myproject