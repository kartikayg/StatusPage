/**
 * @fileoverview
 */

/**
 *
 */
const init = (db, groupRepo) => {

  const fetch = () => {
    console.log('component');
  };

  return Object.create({
    fetch
  });

};

export default Object.create({ init });
